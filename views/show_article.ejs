<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container m-5 border border-dark rounded" style="background-color: #f4f4f4;">
        <% if (article) { %>
        <h1 class="text-center display-3 mb-2 mt-4"><%= article.title %></h1>
        <h3 class="cart-text h5 mt-5 pl-2 lead">Written by: <%= article.user.name %></h3>
        <div class="text-muted text-center pt-4 pb-2 "><%= article.createdAt.toLocaleDateString() %></div>
        <div class="container mt-5 text-center">
            <%- article.sanitizedHtml %>
        </div>
        <img class="img-fluid img-thumbnail rounded mt-5 mb-5" src="<%= article.imagePath %>" alt="Article image" style="width: 100%;">
        <% } else { %>
            <p>Article doesnt exist</p>
          <% } %>
    </div>
    <a class="btn btn-primary m-5" href="/">All Articles</a>
</body>
</html>
