<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Blog | Collection</title>
</head>
<body>
    <a class="m-3 mb-0 btn btn-primary" href="/users/login">Authors Login</a>
    <div class="container pb-4">
        <% articles.forEach((article, index) => { %>
          <% if (index % 2 === 0) { %>
            <div class="row">
          <% } %>
          <div class="col">
            <div class="card mt-4">
              <div class="card-body">
                <h4 class="card-title"><%= article.title %></h4>
                <h6 class="cart-text lead mb-2">Author: <%= article.user.name %></h6>
                <div class="card-subtitle text-muted mt-2 mb-2">
                  <%=article.createdAt.toLocaleDateString()%>
                </div>
                <div class="cart-text mb-2"><%= article.description%></div>
                <a class="btn btn-primary" href="/<%= article.slug %>">Read More</a>
              </div>
            </div>
          </div>
          <% if (index % 2 !== 0 || index === articles.length - 1) { %>
            </div>
          <% } %>
        <% }) %>
      </div>
</body>
</html>