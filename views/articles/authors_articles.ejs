<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Authors | Articles</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4 mt-3">Blog Articles</h1>
        <h2 class="mb-5 mt-3">Welcome <%= name %>!</h2>
        <a href="/articles/new" class="btn btn-success">New Article</a>
        <% articles.forEach(article =>{ %>
            <div class="card mt-4 mb-4">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%=article.createdAt.toLocaleDateString()%>
                    </div>
                    <div class="cart-text mb-2"><%= article.description%></div>
                    <a class="btn btn-primary" href="articles/<%= article.slug %>">Read More</a>
                    <a class="btn btn-info" href="articles/edit/<%= article.id %>">Edit</a>
                    <form class="d-inline" action="/articles/<%= article.id %>?_method=DELETE" method="post">
                    <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        <%  }) %>
        <form class="mt-4 mb-4" action="/logout?_method=DELETE" method="post">
            <button class="btn btn-danger" type="submit">Log Out</button>
        </form>
    </div>
</body>
</html>